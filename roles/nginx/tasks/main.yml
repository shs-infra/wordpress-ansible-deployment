- name: Update apt cache
  apt:
    update_cache: yes

- name: Install nginx
  apt:
    name: nginx
    state: present

- name: Create index page
  copy:
    content: "<h1>Deployed with Ansible</h1>"
    dest: /var/www/html/index.html
  notify: restart nginx